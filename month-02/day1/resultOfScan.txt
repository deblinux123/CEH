User-agent: *
fsocity.dic
key-1-of-3.txt


| مسیر                             | توضیح                                                                                      |
| -------------------------------- | ------------------------------------------------------------------------------------------ |
| `/robots.txt`                    | ✅ **موجوده** و می‌تونه مسیرهای پنهان رو نشون بده – حتماً محتواش رو بررسی کن.               |
| `/admin/`, `/wp-admin/`          | 🔑 مسیر مدیریت WordPress، می‌تونه هدف حمله Brute-force باشه.                               |
| `/wp-login.php`                  | صفحه ورود به WordPress. از اینجا می‌تونی با ابزارهایی مثل `wpscan` یا Hydra استفاده کنی.   |
| `/readme`, `/license`            | ممکنه نسخه CMS یا نکات امنیتی لو رفته باشه.                                                |
| `/intro`                         | **بسیار مهم**، حجم فایل ۵۰۰KB هست، احتمالاً دیتا مخفی یا کلید یا هش درونشه. بررسی کامل کن! |
| `.htaccess`, `.hta`, `.htpasswd` | وجود دارن ولی Forbidden، ولی نشون‌دهنده پیکربندی‌های امنیتی سرور هستن.                     |
| `/phpmyadmin`                    | وجود داره ولی 403، ممکنه بشه با دستکاری URL یا اشتباه پیکربندی وارد شد.                    |
| `/wp-config`                     | معمولاً فایل حساسیه، ولی در اینجا حجمش 0 هست، ممکنه نمایشی باشه.                           |
